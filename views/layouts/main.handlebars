<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>{{title}}</title>
  <link rel="stylesheet" href="/css/jass.css">
</head>

<body class="bg-primary text-light">
 <nav class="bg-dark p-4">
  <div class="m-1">
  <a class="p-1" href="/">Home</a>
  <a class="p-1" href="/dashboard">Dashboard</a>
  {{#if loggedIn}}
    <a id="logout" class="p-1" href="">Logout</a>
  {{else}}
    <a class="p-1" href="/login">Login</a>
  {{/if}}</li>
  </ul>
  </div>
  <div id="site-title">
  <h class="logo"><strong>{{logoTitle}}</strong></h1>
  </div>
 </nav>

  <main class="container container-fluid mt-5">
    <!-- Render the contents of the sub layout here -->
    {{{ body }}}
  </main>
  <footer>
      
  </footer>
  {{#if loggedIn}}
<script src="./js/logout.js"></script>
{{/if}}
</body>

</html>
